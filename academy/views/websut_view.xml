<?xml version="1.0" encoding="UTF-8"?>

<odoo>

    <template id="index">
   
        <t t-call="website.layout">
            
            <t t-set="title">Academy</t>
            <!-- <header id="top" data-anchor="ture" data-name="Header" class="o_header_standard">

            </header>
             -->
            <div class="oe_structure">
               
                <div class="container">
                    
                    <t t-foreach="teachers" t-as="teacher">
                        <p >
                            <a class="ss" t-attf-href="/academy/{{ slug(teacher) }}">
                            <t t-esc="teacher.name"/></a>

                        </p>
                    </t>
                </div>
            </div>
        </t>
    </template>
    <template id="biography">
    
      <t t-call="website.layout">
          <t t-set="title">Academy</t>
          <div class="oe_structure"/>
          <div class="oe_structure">
            <div class="container">
                <h3 t-field="person.name"/>
                <p>Last modified: <i t-field="person.write_date" t-options='{"widget": "relative"}'/></p>
                <p><i t-field="person.create_uid" /> </p>
                <div t-field="person.biography" />
            </div>
        </div>
          <div class="oe_structure"/>
      </t>
    </template>
      

</odoo>
